{
  "name": "hf-26-025-contracts",
  "version": "1.0.0",
  "description": "Solidity smart contracts for HF-26-025",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache",
    "seed": "hardhat run scripts/seed.js",
    "seed:ganache": "hardhat run scripts/seed.js --network ganache",
    "chain:reset": "hardhat run scripts/reset.js --network ganache"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "ganache": "^7.9.2",
    "hardhat": "^2.22.0",
    "hardhat-gas-reporter": "^1.0.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^16.6.1"
  }
}
